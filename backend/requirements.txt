# =============================================================================
# LMIS Backend — Python Dependencies
# Python 3.11+
# =============================================================================

# --- Web Framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17          # file uploads
websockets==13.1

# --- Async / Task Queue ---
celery[redis]==5.4.0
redis==5.2.0
kombu==5.4.2
flower==2.0.1                     # Celery monitoring (dev)

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0                   # async PostgreSQL driver
alembic==1.14.0
psycopg2-binary==2.9.10           # sync driver for Alembic CLI

# --- Pydantic / Config ---
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- Authentication ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.17

# --- Object Storage ---
boto3==1.35.86                    # MinIO / S3 compatible
minio==7.2.11

# --- Vector Database ---
qdrant-client==1.12.1

# --- LLM / AI Orchestration ---
langchain==0.3.13
langchain-core==0.3.28
langchain-anthropic==0.3.5
langchain-openai==0.2.14
langchain-google-genai==2.0.8
langgraph==0.2.60
anthropic==0.40.0
openai==1.58.1
google-generativeai==0.8.3

# --- Embeddings ---
sentence-transformers==3.3.1
transformers==4.47.1
torch==2.5.1+cpu                  # CPU-only for Docker; swap for GPU variant in prod
torchvision==0.20.1+cpu

# --- OCR ---
paddlepaddle>=3.5                 # bumped from 2.6.2 -- old version cannot parse rec_texts/rec_scores 3.x result format used in ocr/engine.py
paddleocr>=3.5                     # bumped from 2.9.1 -- same reason
pdf2image==1.17.0
Pillow==11.0.0
opencv-python-headless==4.10.0.84
pypdf==5.1.0

# --- Handwriting OCR ---
# TrOCR via transformers (already included above)

# --- Medical NLP ---
spacy==3.8.3
scispacy==0.5.5
medspacy==1.3.0

# --- Biomedical NER (models downloaded at runtime via HuggingFace)
# allenai/scibert_scivocab_uncased
# allenai/medicine-scibert
# d4data/biomedical-ner-all

# --- Statistical / Numerical ---
numpy==2.2.0
scipy==1.14.1
pandas==2.2.3
statsmodels==0.14.4               # Mann-Kendall, CUSUM

# --- PHI Redaction ---
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.8.3                      # already listed

# --- Encryption ---
cryptography==44.0.0

# --- HTTP Client ---
httpx==0.28.1
aiohttp==3.11.10

# --- Utilities ---
structlog==24.4.0                 # structured logging
tenacity==9.0.0                   # retry logic
python-dateutil==2.9.0.post0
pytz==2024.2
tqdm==4.67.1
orjson==3.10.12                   # fast JSON
rich==13.9.4                      # pretty CLI output
click==8.1.8                      # CLI commands

# --- File format handling ---
python-magic==0.4.27
chardet==5.2.0
openpyxl==3.1.5                   # Excel lab exports

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1                     # TestClient
factory-boy==3.3.1                # test fixtures
faker==33.1.0

# --- Dev tools ---
black==24.10.0
isort==5.13.2
mypy==1.13.0
ruff==0.8.4